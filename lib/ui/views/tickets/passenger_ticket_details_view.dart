import 'package:flutter/material.dart';

class PassengerTicketDetailsView extends StatelessWidget {
  const PassengerTicketDetailsView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
