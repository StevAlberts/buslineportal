import 'package:flutter/material.dart';

class JourneyReportView extends StatelessWidget {
  const JourneyReportView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
